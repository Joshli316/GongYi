{
  "name": "gongyi",
  "version": "1.0.0",
  "description": "Free bilingual work authorization tracker for F-1 OPT students",
  "scripts": {
    "build": "node build.js",
    "dev": "npm run build && npx http-server dist -p 3000 -c-1",
    "serve": "npx http-server dist -p 3000 -c-1",
    "test": "vitest run"
  },
  "dependencies": {
    "lucide": "^1.7.0"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4.2.2",
    "esbuild": "^0.28.0",
    "jsdom": "^29.0.2",
    "tailwindcss": "^4.2.2",
    "typescript": "^5.6.0",
    "vitest": "^4.1.3"
  }
}
